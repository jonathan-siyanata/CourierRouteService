module.exports = require('./lib/axios');
module.exports = require('mysql');

const app = express();

app.use(express.json());

const db = mysql.createConnection({
    host: "localhost",
    user: "jonathan",
    password: "54321",
    database: "courierrouteservice",
});

app.listen(3306, () =>{
    console.log("running server");
});